# Number schema

A function which creates a parsing rule to ensure that a given value is a number.

## Arguments

1. (Optional): A list of modifiers
    - `min`: [Learn more](/modifiers/min).
    - `max`: [Learn more](/modifiers/max).
    - `custom`: [Learn more](/modifiers/custom).
2. (Optional): A custom object with error messages to be displayed when validation fails or there are type errors.

## Examples

```ts
import { number } from '@nordic-ui/validathor';

const numberRule = number('Value must be a number');

try {
  const parsedValue = numberRule.parse(123);
  console.log('Parsed value:', parsedValue);
} catch (error) {
  console.error('Parsing failed:', error.message);
}
```